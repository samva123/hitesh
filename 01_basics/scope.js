function addone( num){
    return num+1 ;
    // in this type of function we can use them before defining it but in below expression/function we can't use them
}

const addtwo = function(num){
    return num+2;
}